#include<iostream>
using namespace std;
 
int power(int base , int power)
{
    if(power == 0)
    return 1;

     if(power == 1)
    return a;

    //Recursive call
    int ans = power(a , b/2)

    if(b%2==0)
    return ans*ans;

    else{
        return a*ans*ans;
    }

    
}
